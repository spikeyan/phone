/**
 * api urls setting page
 */

const api = 'http://115.159.223.254/';

// const api = '';

const urls = {
    getMangaNews : `${api}get-manga-news?page=`,
    loggedCheck : `${api}login-check`,
    login:`${api}login`
}

export default urls